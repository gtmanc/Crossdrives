package com.example.crossdrives;

import java.util.ArrayList;
import java.util.List;

public interface GlobalConstants {
    /*
     * Currently, this app supports two brand: Google and Microsoft.
     */
    final int MAX_BRAND_SUPPORT = 2; //Google, Microsoft
    final String BRAND_GOOGLE = SignInManager.BRAND_GOOGLE;
    final String BRAND_MS = SignInManager.BRAND_MS;

}
